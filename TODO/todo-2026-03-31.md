Tomorrow's task:

Create the frame/box to improve the camera UI.

Requirements:
- When a face is properly in frame, the outlined frame should turn green.
- When a face is not in frame, the outlined frame should be red.

Implementation details can be handled tomorrow.
=======

* When a face is properly in frame, the outlined frame should turn green.
* When a face is not in frame, the outlined frame should be red.

Implementation details can be handled tomorrow.

done implemented red box so that when user is withing red box it turns a green 

Also added a feature to save the user if the user is unknown once they are in the green box you can save the user and it stor4es users face 

in local database so that when users face is within, the green box it will display the users name at the bottom instead of displaying unknown user 

