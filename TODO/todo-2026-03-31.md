Project notes

Completed

- Added face alignment box feedback:
  - red when face is outside the target area
  - green when face is inside the target area
- Added saved user registration from the camera feed
- Added local face recognition from `user_images/`
- Added barcode scanning with OpenCV's barcode detector
- Added local medicine registry in `medicine_records.json`
- Added face-to-barcode flow:
  - recognize user
  - scan barcode
  - verify barcode belongs to recognized user
  - display medicine details

Tomorrow

- Test barcode scanning with a real medicine package and camera
- Confirm the barcode detector can read the packaging from a practical distance
- Verify the recognized user remains stable while scanning
- Add real barcode entries to `medicine_records.json`
- Check all three outcomes:
  - correct user + correct barcode
  - correct barcode but wrong user
  - unknown barcode
- Note any scan failures caused by blur, glare, distance, or barcode type

After barcode testing

- Decide whether to keep the local JSON registry or move medicine details to an FDA / RxNorm lookup
- Add a UI flow for entering medicine records without editing JSON manually

