Progress log

2026-07-14

Completed today:

- Reviewed the full project structure and confirmed the current runtime flow across face recognition, barcode scanning, and medicine lookup.
- Confirmed the current barcode result logic:
  - matching barcode for the recognized user shows medicine details
  - known barcode for a different user shows an ownership mismatch message
  - unknown barcode shows that no medicine record was found for the recognized user
- Verified the core Python files parse successfully.
- Updated the README so the current medicine lookup behavior is documented.

Next to do:

- Test barcode scanning with a known barcode image displayed on a second monitor or another screen.
- Add at least one second dummy medicine record for another user if you want to test the ownership-mismatch path end to end.
- Confirm OpenCV barcode detection can reliably decode the chosen test image under real camera conditions.
- If barcode scans are unreliable, add temporary debug output showing the decoded barcode value in the UI or terminal.
